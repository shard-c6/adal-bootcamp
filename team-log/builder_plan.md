# Builder Plan — HydraDB Hero Clone

- Scaffold a Vite React app in `hyperspell-clone`.
- Implement a single-page hero clone with componentized React/CSS.
- Use observed HydraDB text and Framer image URLs from fetched page.
- Recreate visual style with CSS gradients, floating image cards, dark nav, CTA, and keyframe animations.
- Validate with npm install/build.
